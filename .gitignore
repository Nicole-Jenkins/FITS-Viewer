# Virtual environment
venv/
.venv/

# Python bytecode
__pycache__/
*.pyc

# Editor / OS junk
.vscode/
.idea/
.DS_Store
Thumbs.db

# App's own on-disk thumbnail cache (regenerates automatically, not source)
# Windows: %LOCALAPPDATA%\FitsViewer\thumbnails
# Linux/Mac: ~/.cache/FitsViewer/thumbnails
# (lives outside the repo, listed here only in case cache_dir is ever
# pointed inside the project folder during testing)
FitsViewer/

# PyInstaller build output (regenerate with FitsViewer.spec, don't commit)
build/
dist/
Test FITS files/
*.fits
*.fit
*.fts
